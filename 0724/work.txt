
ModelDbはtraitの集合とする

propのschema/nameをtable_nameに変更

isValidOrderとか変更したので反映が必要
 ==>集約SQLとvalidateを一緒にする?
 
copyコマンドはpostgresオリジナルとしてtrait

INSERT
    1SQLとするため、NULLでもbindできるか?


もしかしてpostgres専用の部分はtraitではなく
construct injectionで可変にした方が？
    __call()でmethod_exists？

factory？provider?
    ModelDbは継承するのでtraitで良い?

